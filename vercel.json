{
  "version": 2,
  "builds": [
    { "src": "api/*.go", "use": "@vercel/go" }
  ],
  "routes": [
    { "src": "/login", "dest": "api/auth.go" },
    { "src": "/checklist", "dest": "api/checklist.go" }
  ]
}
